<%=
    link_to klass_section_student_path(@klass, @section, student),
    class: "list-group-item student_item",id: dom_id(student),remote: true  do
     content_tag(:span, raw("&nbsp;"), class: "label label-#{student_house_label_class(student)} pull-right") + content_tag(:b,student.roll_number) + " " +
      student.name
  end
%>
